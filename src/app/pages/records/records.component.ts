import { Component,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { ActivatedRoute,RouterLink } from '@angular/router'; import { ApiService } from '../../core/api.service'; import { PetRecord,RecordType } from '../../models/models';
@Component({selector:'app-records',standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`<main class="page"><header class="topbar"><a [routerLink]="['/pets',petId]" class="btn btn-ghost">‹ Prontuário</a><button class="btn btn-primary" (click)="showForm=!showForm">+ Registrar</button></header><span class="eyebrow">{{title}}</span><h1>{{title}}</h1><p class="muted">{{subtitle}}</p><form *ngIf="showForm" class="card stack" (ngSubmit)="save()" style="margin:20px 0"><div class="field"><label>Título *</label><input [(ngModel)]="form.title" name="title" required [placeholder]="placeholder"></div><div class="grid2"><div class="field"><label>Data *</label><input type="date" [(ngModel)]="form.date" name="date" required></div><div class="field"><label>Próxima data</label><input type="date" [(ngModel)]="form.nextDate" name="nextDate"></div></div><div class="grid2" *ngIf="section!=='weight'"><div class="field"><label>Veterinário</label><input [(ngModel)]="form.veterinarian" name="vet"></div><div class="field"><label>Clínica / laboratório</label><input [(ngModel)]="form.clinic" name="clinic"></div></div><div class="field" *ngIf="section==='weight'"><label>Peso (kg)</label><input type="number" step="0.1" [(ngModel)]="weight" name="weight"></div><div class="field"><label>Observações</label><textarea [(ngModel)]="form.notes" name="notes" rows="3"></textarea></div><div class="field" *ngIf="section==='exams'||section==='consultations'||section==='vaccines'"><label>Anexo (imagem/PDF)</label><input type="file" (change)="fileChange($event)" accept="image/*,application/pdf"></div><button class="btn btn-primary" type="submit">Salvar registro</button></form><section class="stack"><article class="card" *ngFor="let r of records"><div class="row between"><div><span class="eyebrow">{{r.date|date:'dd/MM/yyyy'}}</span><h2 style="margin-top:4px">{{r.title}}</h2></div><span class="chip" [class.warn]="r.status==='overdue'">{{r.status||'registrado'}}</span></div><p class="muted">{{r.notes || 'Sem observações'}}</p><div *ngIf="r.attachments?.length"><a *ngFor="let a of r.attachments" [href]="a.url" target="_blank">📎 {{a.name}}</a></div></article><div class="card empty" *ngIf="!records.length">Nenhum registro nesta categoria.</div></section></main>`})
export class RecordsComponent implements OnInit{petId='';section='';title='';subtitle='';placeholder='';showForm=false;records:PetRecord[]=[];file?:File;weight?:number;form:Partial<PetRecord>={title:'',date:new Date().toISOString().slice(0,10)};map:any={consultations:['Consultas','Histórico de consultas, diagnósticos, receitas e retornos.','Consulta veterinária','consultation'],vaccines:['Carteira de vacinação','Vacinas aplicadas e próximas doses.','Vacina V10','vaccine'],exams:['Exames','Resultados, laudos e documentos médicos.','Hemograma','exam'],medications:['Medicamentos','Tratamentos atuais e concluídos.','Prednisona 20 mg','medication'],weight:['Controle de peso','Acompanhe a evolução de peso.','Pesagem','weight'],procedures:['Procedimentos','Cirurgias, internações e procedimentos.','Limpeza dentária','procedure']};constructor(private route:ActivatedRoute,private api:ApiService){} ngOnInit(){this.petId=this.route.snapshot.paramMap.get('id')!;this.section=this.route.snapshot.paramMap.get('section')!;const m=this.map[this.section]||['Histórico','Registros do pet.','Novo registro','note'];[this.title,this.subtitle,this.placeholder]=m;this.load()} load(){const type=this.map[this.section]?.[3];this.api.records(this.petId,type).subscribe(r=>this.records=r)} fileChange(e:Event){this.file=(e.target as HTMLInputElement).files?.[0]} save(){const type=(this.map[this.section]?.[3]||'note') as RecordType;const doCreate=(attachment?:any)=>{const metadata=this.section==='weight'?{weight:this.weight}:undefined;const attachments=attachment?[attachment]:[];this.api.createRecord(this.petId,{...this.form,type,metadata,attachments}).subscribe(()=>{this.showForm=false;this.form={title:'',date:new Date().toISOString().slice(0,10)};this.file=undefined;this.load()})};if(this.file){this.api.upload(this.petId,this.file).subscribe(a=>doCreate(a));}else doCreate();}}
