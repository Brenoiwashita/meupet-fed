import { Component,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { ActivatedRoute,RouterLink } from '@angular/router'; import { ApiService } from '../../core/api.service'; import { Pet,PetRecord } from '../../models/models'; import { BottomNavComponent } from '../../shared/bottom-nav.component';
@Component({selector:'app-pet-detail',standalone:true,imports:[CommonModule,RouterLink,BottomNavComponent],template:`<main class="page" *ngIf="pet"><header class="topbar"><a routerLink="/home" class="btn btn-ghost">‹ Voltar</a><span class="chip">Prontuário</span></header><section class="card"><div class="row"><div class="avatar" style="width:76px;height:76px;font-size:28px">{{pet.name[0]}}</div><div><h1 style="margin:0">{{pet.name}}</h1><div class="muted">{{pet.breed || pet.species}} • {{pet.weight || '—'}} kg</div></div></div><div class="grid2" style="margin-top:18px"><div class="metric"><span class="eyebrow">Alergias</span><div>{{pet.allergies?.join(', ') || 'Nenhuma informada'}}</div></div><div class="metric"><span class="eyebrow">Condições</span><div>{{pet.conditions?.join(', ') || 'Nenhuma informada'}}</div></div></div></section><div style="height:24px"></div><section class="stack"><h2>Prontuário</h2><a class="card row between" *ngFor="let s of sections" [routerLink]="['/pets',pet._id,s.route]" style="text-decoration:none;color:inherit"><div class="row"><div class="quick-icon" style="margin:0">{{s.icon}}</div><div><b>{{s.label}}</b><div class="muted" style="font-size:12px">{{s.help}}</div></div></div><span>›</span></a><h2>Últimos registros</h2><div class="card event" *ngFor="let r of records.slice(0,5)"><div class="datebox"><small>{{r.date|date:'MMM'}}</small><span>{{r.date|date:'dd'}}</span></div><div><span class="eyebrow">{{r.type}}</span><h2>{{r.title}}</h2><p class="muted" style="margin:4px 0 0">{{r.notes}}</p></div></div></section></main><app-bottom-nav/>`}) export class PetDetailComponent implements OnInit{pet?:Pet;records:PetRecord[]=[];sections=[{label:'Consultas',route:'consultations',icon:'🩺',help:'Diagnóstico, receita e retornos'},{label:'Vacinas',route:'vaccines',icon:'💉',help:'Carteira digital e próximas doses'},{label:'Exames',route:'exams',icon:'🧪',help:'Resultados, laudos e imagens'},{label:'Medicamentos',route:'medications',icon:'💊',help:'Tratamentos e uso contínuo'},{label:'Peso',route:'weight',icon:'⚖',help:'Evolução e histórico'},{label:'Procedimentos',route:'procedures',icon:'✚',help:'Cirurgias, internações e outros'}];constructor(private route:ActivatedRoute,private api:ApiService){} ngOnInit(){const id=this.route.snapshot.paramMap.get('id')!;this.api.pet(id).subscribe(p=>this.pet=p);this.api.records(id).subscribe(r=>this.records=r)}}
